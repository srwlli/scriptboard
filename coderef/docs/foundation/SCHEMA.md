# Data Schema

## 🗄️ Database: Not Detected
The application uses local JSON storage under `~/.scriptboard/` for sessions and autosave. No RDBMS/ORM detected.

### Store: `~/.scriptboard/sessions/*.json`
| Field | Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `prompt` | string | | Current prompt text |
| `attachments` | array | | Attachment metadata and content |
| `responses` | array | | Stored LLM responses |
| `favorites` | array | | Favorite folders (label/path) |
| `llm_urls` | array | | LLM provider labels/URLs |

### File: `~/.scriptboard/autosave.json`
| Field | Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `...` | object | | Same shape as session snapshot |

## 🔗 Relationships
- Not Applicable (local JSON files)

## 📜 Enums / Types
- **ErrorCode**: `VALIDATION_ERROR`, `NOT_FOUND`, `CONFLICT`, `INTERNAL_ERROR`, `GIT_ERROR`, `CONFIG_ERROR`, `IO_ERROR`, `SECURITY_ERROR`, `LLM_ERROR`, `UNSUPPORTED`
- **BatchJobStatus**: `PENDING`, `RUNNING`, `COMPLETED`, `FAILED`, `CANCELLED`
- **SearchItemType**: `prompt`, `attachment`, `response`, `favorite`

Note: API request/response shapes are defined in `backend/schemas.py` and exposed via FastAPI.

---
(Generated by Foundation Docs Agent via POWER Protocol)
